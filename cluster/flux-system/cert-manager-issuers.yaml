---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager-issuers
  namespace: flux-system
spec:
  interval: 1h
  path: ./cluster/infrastructure/cert-manager-issuers
  prune: true
  sourceRef:
    kind: GitRepository
    name: k8s-homelab-gitops
    namespace: flux-system
  wait: true
  timeout: 10m
  dependsOn:
    - name: cert-manager-webhook-ionos
      namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
