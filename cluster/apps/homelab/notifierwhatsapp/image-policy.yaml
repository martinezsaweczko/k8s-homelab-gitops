apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: notifierwhatsapp
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: notifierwhatsapp
  policy:
    semver:
      range: ">=1.0.0"
